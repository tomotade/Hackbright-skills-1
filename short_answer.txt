1) What does the `rm` command in the terminal do?

Your answer:

remove files
------------------
2) What terminal command would you use to list all the files and folder in the 
   current directory?

Your answer:

ls -a
------------------
3) Explain the difference between these two commands in the terminal:
   
   $ cd Users
   $ cd /Users

Your answer:
 $ cd Users     will change directory to Users directory provided you are in the same directory or folder.

   $ cd /Users    will take you directly to /Users directory regardless of your current location on the terminal.

------------------
4) Describe what git is and what it is used for.

Your answer:
git is used for version control, and it monitores codes or code changes

------------------
5) Describe what GitHub is and what it is used for.

Your answer:
GitHub is used for storing, tracking, and collaborating on software projects.

------------------
6) Which of these commands sets up a new git repository?
    a) git clone 
    b) git pull
    c) git init
    d) git status

Your answer:
c) git init 
------------------
7) You want to check that you have properly added all the files you want to be
   tracked in git BEFORE you make a commit. Which command will do this?
     a) git push
     b) git commit
     c) git status
     d) git remote

Your answer:

c) git status
------------------
8) What is the difference between `git add` and `git commit`?

Your answer:


git add stages changes for commit, while git commit saves the staged changes to the project history.


------------------
9) What does the `git push` command do?

Your answer: 

git push upload local repository content to a remote repository, like github.
------------------
10) When would you use the `git clone` command?

Your answer: 
 git clone command is used to create a local copy of a remote Git repository. 


